[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-handbook-backtest"
version = "0.1.0"
description = "Standard-library daily quantitative backtesting framework"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["backtest*"]
